# MVC Architecture Diagram

## 📊 Visual Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER (View)                          │
│                                                                 │
│  login.html  │  signup.html  │  dashboard.html  │  buy-stocks.html │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests (JSON)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CONTROLLERS (Handle HTTP)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌────────────────────┐  ┌─────────────┐ │
│  │ AuthController   │  │ PortfolioController│  │StockController│
│  ├──────────────────┤  ├────────────────────┤  ├─────────────┤ │
│  │ POST /login      │  │ GET /api/portfolio │  │GET /api/stocks│
│  │ POST /signup     │  │ POST /api/buy-stock│  └─────────────┘ │
│  │ GET  /logout     │  └────────────────────┘                  │
│  └──────────────────┘                                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Calls
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SERVICES (Business Logic)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌────────────────────┐  ┌─────────────┐ │
│  │  UserService     │  │ PortfolioService   │  │StockService │ │
│  ├──────────────────┤  ├────────────────────┤  ├─────────────┤ │
│  │ authenticate()   │  │ getPortfolioData() │  │searchStocks()│
│  │ registerUser()   │  │ buyStock()         │  │getCurrent   │ │
│  │ userExists()     │  │ getPortfolio()     │  │  Prices()   │ │
│  └──────────────────┘  └────────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Calls
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                 REPOSITORIES (Data Access)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌────────────────────┐  ┌─────────────┐ │
│  │ UserRepository   │  │PortfolioRepository │  │StockRepository│
│  ├──────────────────┤  ├────────────────────┤  ├─────────────┤ │
│  │ findAll()        │  │ getPortfolio()     │  │getAllStocks()│
│  │ findByUsername() │  │ saveAllHoldings()  │  │searchStocks()│
│  │ save()           │  │ hasPortfolio()     │  │fetchCurrent │ │
│  │ authenticate()   │  │                    │  │  Prices()   │ │
│  └──────────────────┘  └────────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Reads/Writes
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATA STORAGE (Files/API)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌────────────────┐  ┌──────────────────────┐│
│  │ users.json   │  │ holdings.json  │  │ stocks.json          ││
│  └──────────────┘  └────────────────┘  └──────────────────────┘│
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         Twelve Data API (External Stock Prices)          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    SUPPORTING COMPONENTS                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ MODELS (Domain Entities)                                 │  │
│  │  • User.java                                             │  │
│  │  • Stock.java                                            │  │
│  │  • Portfolio.java                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DTOs (Data Transfer Objects)                             │  │
│  │  Request:  LoginRequest, BuyStockRequest                 │  │
│  │  Response: LoginResponse, ErrorResponse, StockReference  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ CONFIG (Configuration)                                   │  │
│  │  • AppConfig.java (API keys, constants, defaults)        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example: User Login

```
1. User enters credentials in login.html
   ↓
2. Browser sends: POST /login
   {
     "username": "john",
     "password": "secret"
   }
   ↓
3. AuthController.handleLogin()
   - Deserializes JSON to LoginRequest
   - Calls UserService
   ↓
4. UserService.authenticate(loginRequest)
   - Business logic: validate credentials
   - Calls UserRepository
   ↓
5. UserRepository.authenticate(username, password)
   - Reads users.json file
   - Checks if credentials match
   - Returns true/false
   ↓
6. UserService creates LoginResponse
   - Returns LoginResponse("token", "john")
   ↓
7. AuthController sets session
   - session.attribute("isLoggedIn", true)
   - session.attribute("username", "john")
   - Serializes LoginResponse to JSON
   ↓
8. Browser receives:
   {
     "token": "token",
     "username": "john"
   }
   ↓
9. Browser redirects to dashboard.html
```

---

## 📦 Package Structure

```
org.global.academy/
│
├── Server.java                 # Main application entry point
│
├── config/
│   └── AppConfig.java          # Configuration constants
│
├── model/                      # Domain entities
│   ├── User.java
│   ├── Stock.java
│   └── Portfolio.java
│
├── dto/                        # Data transfer objects
│   ├── request/
│   │   ├── LoginRequest.java
│   │   └── BuyStockRequest.java
│   └── response/
│       ├── LoginResponse.java
│       ├── ErrorResponse.java
│       └── StockReference.java
│
├── controller/                 # HTTP request handlers
│   ├── AuthController.java
│   ├── PortfolioController.java
│   └── StockController.java
│
├── service/                    # Business logic
│   ├── UserService.java
│   ├── PortfolioService.java
│   └── StockService.java
│
└── repository/                 # Data access
    ├── UserRepository.java
    ├── PortfolioRepository.java
    └── StockRepository.java
```

---

## 🎯 Dependency Flow

```
┌────────────┐
│   Server   │  Creates all components
└─────┬──────┘
      │
      ├──→ Creates Repositories
      │    └──→ UserRepository
      │    └──→ PortfolioRepository
      │    └──→ StockRepository
      │
      ├──→ Creates Services (inject repositories)
      │    └──→ UserService(userRepo)
      │    └──→ PortfolioService(portfolioRepo, stockRepo)
      │    └──→ StockService(stockRepo)
      │
      └──→ Creates Controllers (inject services)
           └──→ AuthController(userService, portfolioService)
           └──→ PortfolioController(portfolioService)
           └──→ StockController(stockService)
```









